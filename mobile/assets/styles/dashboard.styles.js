import { StyleSheet } from 'react-native';
import { COLORS } from '../../constants/colors';

// Earnings Dashboard Page Styles (theme-aware)
export const earningsStyles = StyleSheet.create({
  container: { flex: 1, backgroundColor: COLORS.background },
  content: { padding: 16, paddingBottom: 40 },
  center: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  muted: { color: COLORS.textLight, marginTop: 6 },
  error: { color: COLORS.error || '#dc2626', fontWeight: '600', marginBottom: 12 },
  retryBtn: { backgroundColor: COLORS.text, paddingHorizontal: 16, paddingVertical: 8, borderRadius: 10 },
  retryText: { color: COLORS.white, fontWeight: '700' },
  headerRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginBottom: 12 },
  title: { fontSize: 20, fontWeight: '800', color: COLORS.text, letterSpacing: -0.3 },
  refreshBtn: { backgroundColor: COLORS.card, paddingHorizontal: 12, paddingVertical: 6, borderRadius: 10, borderWidth: 1, borderColor: COLORS.border },
  refreshText: { fontSize: 16, fontWeight: '800', color: COLORS.text },

  // Hero summary
  heroCard: {
    backgroundColor: COLORS.card,
    borderRadius: 16,
    padding: 16,
    marginBottom: 16,
    borderWidth: 1,
    borderColor: COLORS.border,
    shadowColor: COLORS.shadow,
    shadowOffset: { width: 0, height: 6 },
    shadowOpacity: 0.08,
    shadowRadius: 8,
    elevation: 3,
  },
  heroValue: { fontSize: 28, fontWeight: '900', color: COLORS.text },
  heroRow: { flexDirection: 'row', gap: 16, marginTop: 10 },
  heroItem: { flex: 1, backgroundColor: COLORS.inputBackground || COLORS.card, borderRadius: 12, paddingVertical: 10, paddingHorizontal: 12, borderWidth: 1, borderColor: COLORS.border },
  heroLabel: { fontSize: 11, color: COLORS.textLight, fontWeight: '600' },
  heroMetric: { fontSize: 15, color: COLORS.text, fontWeight: '800', marginTop: 2 },

  metricsGrid: { flexDirection: 'row', flexWrap: 'wrap', gap: 12, marginBottom: 24 },
  metricCard: {
    width: '47%',
    backgroundColor: COLORS.card,
    padding: 12,
    borderRadius: 14,
    borderWidth: 1,
    borderColor: COLORS.border,
    shadowColor: COLORS.shadow,
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.06,
    shadowRadius: 6,
    elevation: 2,
  },
  metricLabel: { fontSize: 11, fontWeight: '700', color: COLORS.textLight, letterSpacing: 0.1 },
  metricValue: { fontSize: 16, fontWeight: '800', color: COLORS.text, marginTop: 4 },

  sectionHeading: { fontSize: 16, fontWeight: '800', color: COLORS.text, marginBottom: 12 },
  chartRow: {
    flexDirection: 'row',
    alignItems: 'flex-end',
    justifyContent: 'space-between',
    padding: 12,
    backgroundColor: COLORS.card,
    borderRadius: 14,
    borderWidth: 1,
    borderColor: COLORS.border,
    marginBottom: 24,
  },
  barWrapper: { alignItems: 'center', flex: 1 },
  bar: { width: 14, backgroundColor: COLORS.primary, borderTopLeftRadius: 4, borderTopRightRadius: 4, alignSelf: 'center' },
  barLabel: { fontSize: 9, color: COLORS.textLight, marginTop: 4 },

  listingRow: {
    flexDirection: 'row',
    backgroundColor: COLORS.card,
    padding: 12,
    borderRadius: 14,
    marginBottom: 10,
    alignItems: 'center',
    borderWidth: 1,
    borderColor: COLORS.border,
    shadowColor: COLORS.shadow,
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.05,
    shadowRadius: 4,
    elevation: 1,
  },
  listingTitle: { fontSize: 13, fontWeight: '700', color: COLORS.text },
  listingSub: { fontSize: 11, color: COLORS.textLight, marginTop: 2 },
  lastOrder: { fontSize: 10, color: COLORS.textLight, marginTop: 2 },
  qtyBlock: { width: 56, alignItems: 'center', justifyContent: 'center', backgroundColor: COLORS.border, paddingVertical: 10, borderRadius: 12, marginLeft: 12 },
  qtyValue: { fontSize: 16, fontWeight: '800', color: COLORS.text },
  qtyLabel: { fontSize: 10, color: COLORS.textLight },
});