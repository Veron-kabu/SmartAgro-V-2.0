import { StyleSheet } from 'react-native';
import { COLORS } from '../../../constants/colors';

// Export COLORS for use in component
export { COLORS };

export const messagesTabStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: COLORS.background,
  },
  // Header styling similar to favorites
  header: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    paddingHorizontal: 20,
    paddingVertical: 16,
    backgroundColor: COLORS.background,
  },
  title: {
    fontSize: 32,
    fontWeight: "800",
    color: COLORS.text,
    letterSpacing: -0.5,
  },
  headerActions: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  headerButton: {
    width: 40,
    height: 40,
    borderRadius: 20,
    backgroundColor: COLORS.card,
    justifyContent: 'center',
    alignItems: 'center',
    marginLeft: 8,
    shadowColor: COLORS.shadow,
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 2,
  },
  headerButtonsContainer: {
    flexDirection: 'row',
  },
  headerBackButton: {
    marginLeft: 15,
  },
  scrollToBottomButton: {
    backgroundColor: COLORS.primary,
    borderRadius: 20,
  },
  // Header styling
  headerStyle: {
    backgroundColor: COLORS.primary,
    shadowColor: COLORS.shadow,
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.25,
    shadowRadius: 3.84,
    elevation: 5,
  },
  headerTitleStyle: {
    fontWeight: '700',
    fontSize: 18,
  },
  // Icon colors
  headerIconColor: COLORS.white,
  avatarIconColor: COLORS.white,
  emptyStateIconColor: COLORS.primary,
  chevronIconColor: COLORS.white,
  loadingColor: COLORS.primary,
  chatContainer: {
    flex: 1,
    backgroundColor: COLORS.background,
  },
  messagesContainer: {
    backgroundColor: COLORS.background,
    paddingHorizontal: 8,
    paddingTop: 8,
  },
  textInput: {
    backgroundColor: COLORS.inputBackground,
    borderRadius: 20,
    borderWidth: 1,
    borderColor: COLORS.inputBorder,
    paddingHorizontal: 16,
    paddingVertical: 10,
    marginHorizontal: 12,
    marginBottom: 8,
    fontSize: 16,
    color: COLORS.text,
    maxHeight: 100,
  },
  // Custom GiftedChat bubble styles
  bubbleLeft: {
    backgroundColor: COLORS.chatBubbleOther,
    borderRadius: 20,
    borderBottomLeftRadius: 4,
    marginVertical: 2,
    marginHorizontal: 8,
    paddingHorizontal: 12,
    paddingVertical: 8,
    maxWidth: '80%',
  },
  bubbleRight: {
    backgroundColor: COLORS.chatBubbleUser,
    borderRadius: 20,
    borderBottomRightRadius: 4,
    marginVertical: 2,
    marginHorizontal: 8,
    paddingHorizontal: 12,
    paddingVertical: 8,
    maxWidth: '80%',
  },
  textLeft: {
    color: COLORS.chatBubbleTextOther,
    fontSize: 16,
    lineHeight: 20,
  },
  textRight: {
    color: COLORS.chatBubbleText,
    fontSize: 16,
    lineHeight: 20,
  },
  timeText: {
    color: COLORS.messageTime,
    fontSize: 11,
    marginTop: 4,
  },
  inputToolbar: {
    backgroundColor: COLORS.white,
    borderTopWidth: 1,
    borderTopColor: COLORS.divider,
    paddingVertical: 8,
    paddingHorizontal: 8,
    minHeight: 60,
  },
  sendButton: {
    backgroundColor: COLORS.sendButton,
    borderRadius: 20,
    width: 40,
    height: 40,
    justifyContent: 'center',
    alignItems: 'center',
    marginLeft: 8,
    marginBottom: 4,
  },
  sendButtonText: {
    color: COLORS.white,
    fontSize: 16,
    fontWeight: '600',
  },
  composerContainer: {
    flexDirection: 'row',
    alignItems: 'flex-end',
    paddingVertical: 12,
    paddingHorizontal: 8,
    backgroundColor: COLORS.background,
    borderTopWidth: 1,
    borderTopColor: COLORS.border,
    minHeight: 70,
    shadowColor: COLORS.shadow,
    shadowOffset: {
      width: 0,
      height: -2,
    },
    shadowOpacity: 0.05,
    shadowRadius: 3,
    elevation: 2,
  },
  avatar: {
    width: 32,
    height: 32,
    borderRadius: 16,
    backgroundColor: COLORS.primary,
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: 8,
  },
  chatAvatarText: {
    color: COLORS.white,
    fontSize: 14,
    fontWeight: '600',
  },
  list: {
    flex: 1,
    backgroundColor: COLORS.background,
  },
  chatRoomItem: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 12,
    paddingHorizontal: 20,
    backgroundColor: COLORS.white,
  },
  avatarContainer: {
    width: 56,
    height: 56,
    borderRadius: 28,
    marginRight: 16,
    position: 'relative',
  },
  profileImage: {
    width: 56,
    height: 56,
    borderRadius: 28,
    backgroundColor: COLORS.primary,
    justifyContent: 'center',
    alignItems: 'center',
  },
  avatarText: {
    color: COLORS.white,
    fontSize: 20,
    fontWeight: '600',
  },
  onlineIndicator: {
    position: 'absolute',
    bottom: 2,
    right: 2,
    width: 16,
    height: 16,
    borderRadius: 8,
    backgroundColor: COLORS.online,
    borderWidth: 2,
    borderColor: COLORS.white,
  },
  chatRoomInfo: {
    flex: 1,
    justifyContent: 'center',
  },
  chatRoomName: {
    fontSize: 16,
    fontWeight: '600',
    color: COLORS.text,
    marginBottom: 2,
  },
  lastMessage: {
    fontSize: 14,
    color: COLORS.messagePreview,
    lineHeight: 18,
  },
  noMessageText: {
    fontSize: 14,
    color: COLORS.messagePreview,
    fontStyle: 'italic',
    lineHeight: 18,
  },
  chatRoomMeta: {
    alignItems: 'flex-end',
    justifyContent: 'flex-start',
    minWidth: 60,
  },
  timestamp: {
    fontSize: 12,
    color: COLORS.timestamp,
    marginBottom: 4,
  },
  unreadBadge: {
    backgroundColor: COLORS.unreadBadge,
    borderRadius: 10,
    minWidth: 20,
    height: 20,
    justifyContent: 'center',
    alignItems: 'center',
    paddingHorizontal: 6,
  },
  unreadText: {
    color: COLORS.white,
    fontSize: 12,
    fontWeight: '600',
  },
  divider: {
    height: 0.5,
    backgroundColor: COLORS.divider,
    marginLeft: 92, // Align with text content
  },
  emptyContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 40,
    backgroundColor: COLORS.background,
  },
  emptyIconContainer: {
    width: 100,
    height: 100,
    borderRadius: 50,
    backgroundColor: COLORS.background,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 24,
  },
  emptyText: {
    fontSize: 20,
    fontWeight: '400',
    color: COLORS.text,
    marginBottom: 8,
    textAlign: 'center',
  },
  emptySubtext: {
    fontSize: 14,
    color: COLORS.textLight,
    textAlign: 'center',
    marginBottom: 24,
    lineHeight: 20,
    paddingHorizontal: 40,
  },
  startChatButton: {
    backgroundColor: COLORS.primary,
    paddingHorizontal: 24,
    paddingVertical: 12,
    borderRadius: 24,
    shadowColor: COLORS.shadow,
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  startChatButtonText: {
    color: COLORS.white,
    fontSize: 16,
    fontWeight: '500',
    textAlign: 'center',
  },
  loadingContainer: {
    backgroundColor: COLORS.card,
    borderRadius: 20,
    padding: 20,
    margin: 20,
    shadowColor: COLORS.shadow,
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.15,
    shadowRadius: 8,
    elevation: 5,
  },
  loadingText: {
    fontSize: 18,
    fontWeight: '600',
    color: COLORS.text,
    marginTop: 16,
    textAlign: 'center',
  },
  loadingSubtext: {
    fontSize: 14,
    color: COLORS.textLight,
    marginTop: 8,
    textAlign: 'center',
  },
  // GiftedChat customizations
  giftedChatContainer: {
    backgroundColor: COLORS.background,
  },
  messageContainer: {
    backgroundColor: COLORS.card,
    borderRadius: 20,
    marginHorizontal: 8,
    marginVertical: 4,
    padding: 12,
    shadowColor: COLORS.shadow,
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  userMessageContainer: {
    backgroundColor: COLORS.primary,
  },
  messageText: {
    fontSize: 16,
    lineHeight: 22,
  },
  userMessageText: {
    color: COLORS.white,
  },
  otherMessageText: {
    color: COLORS.text,
  },
});