import { StyleSheet } from "react-native";
import { COLORS } from "../../../constants/colors";

export const cartStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: COLORS.background,
  },
  header: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    paddingHorizontal: 20,
    paddingVertical: 16,
  },
  title: {
    fontSize: 32,
    fontWeight: "800",
    color: COLORS.text,
    letterSpacing: -0.5,
  },
  cartSection: {
    backgroundColor: COLORS.card,
    marginHorizontal: 20,
    marginBottom: 16,
    borderRadius: 16,
    borderWidth: 1,
    borderColor: COLORS.border,
    shadowColor: COLORS.shadow,
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.05,
    shadowRadius: 4,
    elevation: 2,
  },
  cartHeader: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    padding: 16,
    borderBottomWidth: 1,
    borderBottomColor: COLORS.border,
  },
  cartHeaderLeft: {
    flexDirection: "row",
    alignItems: "center",
    flex: 1,
  },
  cartIcon: {
    marginHorizontal: 8,
  },
  cartTitle: {
    fontSize: 16,
    fontWeight: "600",
    color: COLORS.text,
  },
  cartActions: {
    flexDirection: "row",
    alignItems: "center",
    gap: 12,
  },
  
  cartContent: {
    padding: 16,
  },
  cartItemContainer: {
    overflow: "hidden",
    marginBottom: 12,
  },
  cartItem: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: COLORS.background,
    borderRadius: 12,
    padding: 12,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  cartItemImage: {
    width: 60,
    height: 60,
    borderRadius: 8,
    backgroundColor: COLORS.border,
  },
  cartItemInfo: {
    flex: 1,
    marginLeft: 12,
  },
  cartItemName: {
    fontSize: 14,
    fontWeight: "600",
    color: COLORS.text,
    marginBottom: 4,
  },
  cartItemPrice: {
    fontSize: 12,
    color: COLORS.textLight,
    marginBottom: 8,
  },
  quantityControls: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: COLORS.card,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: COLORS.border,
    alignSelf: "flex-start",
  },
  quantityBtn: {
    padding: 8,
    minWidth: 32,
    alignItems: "center",
  },
  quantityText: {
    fontSize: 14,
    fontWeight: "600",
    color: COLORS.text,
    marginHorizontal: 8,
  },
  cartItemRight: {
    alignItems: "flex-end",
    marginLeft: 12,
  },
  cartItemSubtotal: {
    fontSize: 14,
    fontWeight: "700",
    color: COLORS.text,
  },
  checkoutButton: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: COLORS.primary,
    borderRadius: 12,
    padding: 16,
    marginTop: 16,
  },
  checkoutIcon: { marginRight: 8 },
  checkoutText: { color: COLORS.white, fontSize: 16, fontWeight: "700" },
  swipeDeleteLayer: {
    position: "absolute",
    right: 0,
    top: 0,
    bottom: 0,
    width: 120,
    backgroundColor: COLORS.error,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 12,
  },
  deleteBtn: { flex: 1, alignItems: "center", justifyContent: "center", width: "100%" },
  deleteBtnText: { color: COLORS.white, fontWeight: "600", fontSize: 12, marginTop: 4 },

  undoSnackbar: {
    position: "absolute",
    left: 20,
    right: 20,
    bottom: 20,
    backgroundColor: COLORS.card,
    borderRadius: 12,
    padding: 16,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    borderWidth: 1,
    borderColor: COLORS.border,
    shadowColor: COLORS.shadow,
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.1,
    shadowRadius: 8,
    elevation: 4,
  },
  undoContent: { flexDirection: "row", alignItems: "center", flex: 1 },
  undoText: { color: COLORS.text, fontSize: 14, marginLeft: 8 },
  undoButton: { backgroundColor: COLORS.primary, paddingHorizontal: 16, paddingVertical: 8, borderRadius: 8 },
  undoButtonText: { color: COLORS.white, fontWeight: "600", fontSize: 12 },
  // Per-item product wrapper used in grid
  productWrapper: {
    width: '47%',
    marginBottom: 12,
  },
  buyButton: {
    marginTop: 8,
    backgroundColor: COLORS.primary,
    paddingVertical: 8,
    borderRadius: 8,
    alignItems: 'center',
  },
  buyButtonText: {
    color: COLORS.white,
    fontWeight: '700',
  },
  checkoutWrap: {
    paddingHorizontal: 20,
    paddingBottom: 12,
  },
  buyRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginTop: 8,
  },
  removeBtn: {
    marginLeft: 8,
    paddingHorizontal: 10,
    paddingVertical: 8,
    borderRadius: 8,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  removeBtnText: { color: COLORS.error, fontWeight: '700' },
});
